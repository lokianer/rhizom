name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  # The root "prepare" script would otherwise write git hooks into the runner's checkout.
  SKIP_INSTALL_SIMPLE_GIT_HOOKS: '1'

jobs:
  check:
    name: check (${{ matrix.os }}, node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: [22, 24]
    defaults:
      run:
        # Git Bash on Windows, bash elsewhere: one syntax for every runner.
        shell: bash
    steps:
      - uses: actions/checkout@v7

      # Must run before setup-node: `cache: pnpm` calls `pnpm store path`.
      # The pnpm version comes from the "packageManager" field in package.json.
      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node ${{ matrix.node }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: pnpm run format:check

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      - name: Unit tests with coverage
        run: pnpm run test:coverage

      - name: Build
        run: pnpm run build

      - name: Assemble the landing page
        run: pnpm run site:build

      # --with-deps installs system packages on Linux and Windows Server; it is a no-op on macOS.
      - name: Install Playwright Chromium
        run: pnpm --filter @rhizom/web exec playwright install --with-deps chromium

      - name: End-to-end tests
        run: pnpm run e2e

      # One package serves every operating system: the only native dependency ships a prebuilt
      # binary for each of them. Assembled once, on the runner whose build the e2e tests just
      # exercised.
      - name: Package a runnable build
        if: matrix.os == 'ubuntu-latest' && matrix.node == 24
        # The two halves of `pnpm run package`, run directly: a `pnpm run` that shells out to a
        # second pnpm hands it the inner package's bin directory, and the root `prepare` script
        # then looks for its tool on a PATH that does not have it.
        run: |
          pnpm --filter @rhizom/server deploy --prod --config.node-linker=hoisted dist/package/server
          node scripts/package-app.mjs

      # The package has to start, not merely exist: the deploy tree is assembled differently
      # from the one the tests ran against.
      - name: Smoke-test the package
        if: matrix.os == 'ubuntu-latest' && matrix.node == 24
        run: |
          cd dist/package/server
          RHIZOM_VAULT_DIR=../example-vault RHIZOM_DATA_DIR=../data PORT=3838 node dist/server.js &
          for _ in $(seq 1 30); do
            curl -sf http://127.0.0.1:3838/api/health && break
            sleep 1
          done
          echo
          curl -sf http://127.0.0.1:3838/api/health | grep '"status":"ok"'
          curl -sf http://127.0.0.1:3838/api/vault | grep '"noteCount":38'
          test "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3838/)" = 200
          test "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3838/api/docs)" = 200
          kill %1

      - name: Upload the runnable build
        if: matrix.os == 'ubuntu-latest' && matrix.node == 24
        uses: actions/upload-artifact@v7
        with:
          name: rhizom-${{ github.sha }}
          path: dist/package/
          retention-days: 30

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report-${{ matrix.os }}-node${{ matrix.node }}
          path: apps/web/playwright-report/
          if-no-files-found: ignore
          retention-days: 14

  docker:
    name: docker build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build the image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          load: true
          tags: rhizom:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke-test the container
        run: |
          docker run -d --name rhizom -p 3737:3737 rhizom:ci
          for _ in $(seq 1 30); do
            curl -sf http://127.0.0.1:3737/api/health && break
            sleep 1
          done
          echo
          docker logs rhizom
          curl -sf http://127.0.0.1:3737/api/health | grep '"status":"ok"'
          curl -sf http://127.0.0.1:3737/api/vault | grep '"noteCount":0'
          test "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3737/api/openapi.json)" = 200
          test "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3737/)" = 200
          test "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3737/notes/does-not-exist)" = 200
          test "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3737/api/nope)" = 404
          docker rm -f rhizom
